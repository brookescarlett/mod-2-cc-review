<h1>PARTY INDEX</h1>

<% @parties.each do |party| %>
  <%= link_to party.host, party %> | <%= link_to "edit", edit_party_path(party) %> | <%= link_to "delete", party_path(party), method: :delete %> 
<% end %>
